name: Java CI with Gradle

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
    - uses: actions/checkout@v6.0.3

    - name: Set up JDK 25
      uses: actions/setup-java@v5.6.0
      with:
        java-version: '25'
        distribution: 'temurin' # Temurin provides stable LTS builds for Java 25
        cache: 'gradle'        # Built-in caching for faster builds

    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v6

    - name: Build with Gradle Wrapper
      run: ./gradlew build
